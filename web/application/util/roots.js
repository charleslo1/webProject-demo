/**
 * roots
 * @type {Object}
 */
module.exports = {
	// browser roots
	win: window,
	$win: $(window),
	
	// dom roots
	html: document.documentElement,
	body: document.body,
	doc: document,
	$doc: $(document),
	$html: $('html'),
	$body: $('body')
};